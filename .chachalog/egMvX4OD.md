---
# Allowed version bumps: patch, minor, major
jahia-private: patch
---

Deprecated `SrcSetURLReplacer.getURLsFromSrcSet(String)` in favor of `SrcSetURLReplacer.parseSrcsetEntries(String)` to offer a more precise extraction of both URLs and descriptors.
