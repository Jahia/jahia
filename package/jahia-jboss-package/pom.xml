<?xml version="1.0" encoding="UTF-8"?>
<project>
    <parent>
        <groupId>org.jahia.server</groupId>
        <artifactId>jahia-package</artifactId>
        <version>6.5-SNAPSHOT</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>
    <artifactId>jahia-jboss-package</artifactId>
    <name>Jahia JBoss Package</name>
    <packaging>pom</packaging>

    <dependencies>
        <dependency>
            <groupId>org.jahia.server</groupId>
            <artifactId>config</artifactId>
            <version>${jahia.package.version}</version>
            <classifier>${jahia.war.classifier}</classifier>
            <type>war</type>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <version>2.1</version>
                <executions>
                    <execution>
                <id>unpack</id>
                <phase>package</phase>
                <goals>
                    <goal>unpack</goal>
                </goals>
                <configuration>
                    <artifactItems>
                        <artifactItem>
                            <groupId>org.jboss</groupId>
                            <artifactId>jbossas</artifactId>
                            <version>${jahia.jboss.version}-jdk6</version>
                            <type>zip</type>
                            <overWrite>false</overWrite>
                            <outputDirectory>${project.build.directory}</outputDirectory>
                            <excludes>**/docs/**,**/server/all/**,**/server/minimal/**,**/ROOT.war/**</excludes>
                        </artifactItem>
                        <artifactItem>
                            <groupId>org.jahia.server</groupId>
                            <artifactId>config</artifactId>
                            <version>${jahia.package.version}</version>
                            <classifier>${jahia.war.classifier}</classifier>
                            <type>war</type>
                            <overWrite>false</overWrite>
                            <outputDirectory>${project.build.directory}/jboss-${jahia.jboss.version}/server/default/deploy/config.war</outputDirectory>
                        </artifactItem>
                    </artifactItems>
                </configuration>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <artifactId>maven-assembly-plugin</artifactId>
                <configuration>
                    <finalName>Jahia_CE_v${jahia.package.version}-${buildNumber}_JBoss_${jahia.jboss.version}</finalName>
                    <descriptors>
                        <descriptor>src/main/assembly/src.xml</descriptor>
                    </descriptors>
                    <appendAssemblyId>false</appendAssemblyId>
                    <tarLongFileMode>gnu</tarLongFileMode>
                </configuration>

                <executions>
                    <execution>
                <id>make-assembly</id>
                <phase>package</phase>
                <goals>
                    <goal>single</goal>
                </goals>
                    </execution>
                </executions>
            </plugin>

        </plugins>
    </build>
</project>
