
<ehcache xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="ehcache.xsd">
    <!--
    DiskStore configuration

    Sets the path to the directory where cache files are created.

    If the path is a Java System Property it is replaced by its value in the
    running VM.

    The following properties are translated:
    * user.home - User's home directory
    * user.dir - User's current working directory
    * java.io.tmpdir - Default temp file path

    Subdirectories can be specified below the property e.g. java.io.tmpdir/one
    -->
    <diskStore path="java.io.tmpdir/jahia-caches"/>
    <!--
    Specifies a CacheManagerEventListenerFactory, be used to create a CacheManagerPeerProvider,
    which is notified when Caches are added or removed from the CacheManager.

    The attributes of CacheManagerEventListenerFactory are:
    * class - a fully qualified factory class name
    * properties - comma separated properties having meaning only to the factory.

    Sets the fully qualified class name to be registered as the CacheManager event listener.

    The events include:
    * adding a Cache
    * removing a Cache

    Callbacks to listener methods are synchronous and unsynchronized. It is the responsibility
    of the implementer to safely handle the potential performance and thread safety issues
    depending on what their listener is doing.

    If no class is specified, no listener is created. There is no default.
    -->
    <cacheManagerEventListenerFactory class="" properties=""/>
    <!-- Uncomment and configure this to activate clustering -->
    <cacheManagerPeerProviderFactory
    class="net.sf.ehcache.distribution.jgroups.JGroupsCacheManagerPeerProviderFactory"
    properties="connect=TCP_NIO(start_port=${cluster.tcp.ehcache.jahia.port};bind_addr=${cluster.tcp.start.ip_address};loopback=true;recv_buf_size=20000000;send_buf_size=640000;discard_incompatible_packets=true;max_bundle_size=64000;max_bundle_timeout=30;use_incoming_packet_handler=true;enable_bundling=true;use_send_queues=false;sock_conn_timeout=300;skip_suspected_members=true;use_concurrent_stack=true):
        TCPPING(initial_hosts=${cluster.tcp.ehcache.jahia.nodes.ip_address};port_range=10;timeout=3000;num_initial_members=${cluster.tcp.num_initial_members}):
        MERGE2(max_interval=100000;min_interval=20000):
FD_SOCK:
FD(timeout=10000;max_tries=5;shun=true):
VERIFY_SUSPECT(timeout=1500):
pbcast.NAKACK(gc_lag=100;retransmit_timeout=3000;discard_delivered_msgs=true):
pbcast.STABLE:
pbcast.GMS(join_timeout=5000;shun=true;print_local_addr=true):
VIEW_SYNC(avg_send_interval=60000):
FC(max_credits=2000000;min_threshold=0.10):
FRAG2(frag_size=60000)"
    propertySeparator="::" />



    <!-- Cache configuration.

    The following attributes are required.

    name:
    Sets the name of the cache. This is used to identify the cache. It must be unique.

    maxElementsInMemory:
    Sets the maximum number of objects that will be created in memory

    maxElementsOnDisk:
    Sets the maximum number of objects that will be maintained in the DiskStore
    The default value is zero, meaning unlimited.

    eternal:
    Sets whether elements are eternal. If eternal,  timeouts are ignored and the
    element is never expired.

    overflowToDisk:
    Sets whether elements can overflow to disk when the memory store
    has reached the maxInMemory limit.

    The following attributes are optional.

    timeToIdleSeconds:
    Sets the time to idle for an element before it expires.
    i.e. The maximum amount of time between accesses before an element expires
    Is only used if the element is not eternal.
    Optional attribute. A value of 0 means that an Element can idle for infinity.
    The default value is 0.

    timeToLiveSeconds:
    Sets the time to live for an element before it expires.
    i.e. The maximum time between creation time and when an element expires.
    Is only used if the element is not eternal.
    Optional attribute. A value of 0 means that and Element can live for infinity.
    The default value is 0.

    diskPersistent:
    Whether the disk store persists between restarts of the Virtual Machine.
    The default value is false.

    diskExpiryThreadIntervalSeconds:
    The number of seconds between runs of the disk expiry thread. The default value
    is 120 seconds.

    memoryStoreEvictionPolicy:
    Policy would be enforced upon reaching the maxElementsInMemory limit. Default
    policy is Least Recently Used (specified as LRU). Other policies available -
    First In First Out (specified as FIFO) and Less Frequently Used
    (specified as LFU)

    Cache elements can also contain sub elements which take the same format of a factory class
    and properties. Defined sub-elements are:

    * cacheEventListenerFactory - Enables registration of listeners for cache events, such as
      put, remove, update, and expire.

    * bootstrapCacheLoaderFactory - Specifies a BootstrapCacheLoader, which is called by a
      cache on initialisation to prepopulate itself.

    Each cache that will be distributed needs to set a cache event listener which replicates
    messages to the other CacheManager peers. For the built-in RMI implementation this is done
    by adding a cacheEventListenerFactory element of type RMICacheReplicatorFactory to each
    distributed cache's configuration as per the following example:

    <cacheEventListenerFactory class="net.sf.ehcache.distribution.RMICacheReplicatorFactory"
         properties="replicateAsynchronously=true,
         replicatePuts=true,
         replicateUpdates=true,
         replicateUpdatesViaCopy=true,
         replicateRemovals=true
         asynchronousReplicationIntervalMillis=<number of milliseconds> "/>

    The RMICacheReplicatorFactory recognises the following properties:

    * replicatePuts=true|false - whether new elements placed in a cache are
      replicated to others. Defaults to true.

    * replicateUpdates=true|false - whether new elements which override an
      element already existing with the same key are replicated. Defaults to true.

    * replicateRemovals=true - whether element removals are replicated. Defaults to true.

    * replicateAsynchronously=true | false - whether replications are
      asynchronous (true) or synchronous (false). Defaults to true.

    * replicateUpdatesViaCopy=true | false - whether the new elements are
      copied to other caches (true), or whether a remove message is sent. Defaults to true.

    * asynchronousReplicationIntervalMillis=<number of milliseconds> - The asynchronous
      replicator runs at a set interval of milliseconds. The default is 1000. The minimum
      is 10. This property is only applicable if replicateAsynchronously=true

    The RMIBootstrapCacheLoader bootstraps caches in clusters where RMICacheReplicators are
    used. It is configured as per the following example:

    <bootstrapCacheLoaderFactory
        class="net.sf.ehcache.distribution.RMIBootstrapCacheLoaderFactory"
        properties="bootstrapAsynchronously=true, maximumChunkSizeBytes=5000000"/>

    The RMIBootstrapCacheLoaderFactory recognises the following optional properties:

    * bootstrapAsynchronously=true|false - whether the bootstrap happens in the background
      after the cache has started. If false, bootstrapping must complete before the cache is
      made available. The default value is true.

    * maximumChunkSizeBytes=<integer> - Caches can potentially be very large, larger than the
      memory limits of the VM. This property allows the bootstraper to fetched elements in
      chunks. The default chunk size is 5000000 (5MB).

    --><!--
    Mandatory Default Cache configuration. These settings will be applied to caches
    created programmtically using CacheManager.add(String cacheName)
    -->
    <defaultCache
            maxElementsInMemory="1000"
            eternal="false"
            timeToIdleSeconds="3600"
            timeToLiveSeconds="14400"
            overflowToDisk="true"
            maxElementsOnDisk="10000"
            diskPersistent="false"
            diskExpiryThreadIntervalSeconds="600"
            memoryStoreEvictionPolicy="LRU"
            >
    </defaultCache>
    <!-- This cache parameters define how many pages do we want to cache in memory/disk -->
    <cache
            name="SkeletonCache"
            overflowToDisk="true"
            maxElementsInMemory="2000"
            timeToIdleSeconds="3600"
            timeToLiveSeconds="14400"
            diskPersistent="true"
            maxElementsOnDisk="10000"
            eternal="false">
        <cacheEventListenerFactory class="org.jahia.services.cache.ehcache.LoggingCacheEventListenerFactory"
                                   properties=""/>
        <cacheEventListenerFactory class="org.jahia.services.cache.ehcache.SkeletonCacheEventListenerFactory"
                                   properties=""/>
                <cacheEventListenerFactory class="net.sf.ehcache.distribution.jgroups.JGroupsCacheReplicatorFactory"
                                   properties="replicateAsynchronously=true, replicatePuts=true, replicateUpdates=true, replicateUpdatesViaCopy=false, replicateRemovals=true"/>
    </cache>
    <cache
            name="SkeletonCache-groups"
            overflowToDisk="true"
            maxElementsInMemory="500"
            diskPersistent="true"
            eternal="false">
        <cacheEventListenerFactory class="org.jahia.services.cache.ehcache.LoggingCacheEventListenerFactory"
                                   properties=""/>
                <cacheEventListenerFactory class="net.sf.ehcache.distribution.jgroups.JGroupsCacheReplicatorFactory"
                                   properties="replicateAsynchronously=true, replicatePuts=true, replicateUpdates=true, replicateUpdatesViaCopy=false, replicateRemovals=true"/>
    </cache>
    <!-- This cache parameters define how many fragments of pages do we want to store in cache (based on an average of 5 fragments per page)-->
    <cache
            name="ContainerHTMLCache"
            overflowToDisk="true"
            maxElementsInMemory="10000"
            diskPersistent="true"
            timeToIdleSeconds="3600"
            timeToLiveSeconds="14400"
            maxElementsOnDisk="50000"
            eternal="false">
        <cacheEventListenerFactory class="org.jahia.services.cache.ehcache.LoggingCacheEventListenerFactory"
                                   properties=""/>
                <cacheEventListenerFactory class="net.sf.ehcache.distribution.jgroups.JGroupsCacheReplicatorFactory"
                                   properties="replicateAsynchronously=true, replicatePuts=true, replicateUpdates=true, replicateUpdatesViaCopy=false, replicateRemovals=true"/>
    </cache>
    <cache
            name="ContainerHTMLCache-groups"
            overflowToDisk="true"
            maxElementsInMemory="500"
            diskPersistent="true"
            eternal="false">
        <cacheEventListenerFactory class="org.jahia.services.cache.ehcache.LoggingCacheEventListenerFactory"
                                   properties=""/>
                <cacheEventListenerFactory class="net.sf.ehcache.distribution.jgroups.JGroupsCacheReplicatorFactory"
                                   properties="replicateAsynchronously=true, replicatePuts=true, replicateUpdates=true, replicateUpdatesViaCopy=false, replicateRemovals=true"/>
    </cache>
    <cache
            name="LockAlreadyAcquiredMap"
            overflowToDisk="true"
            maxElementsInMemory="300"
            diskPersistent="false"
            eternal="true">
        <!-- Uncomment the following to help debug cluster messaging -->
        <!--cacheEventListenerFactory class="org.jahia.services.cache.ehcache.LoggingCacheEventListenerFactory"
                                   properties=""/-->
                <cacheEventListenerFactory class="net.sf.ehcache.distribution.jgroups.JGroupsCacheReplicatorFactory"
                                   properties="replicateAsynchronously=true, replicatePuts=true, replicateUpdates=true, replicateUpdatesViaCopy=false, replicateRemovals=true"/>
    </cache>
    <cache
            name="LockAlreadyAcquiredMap-groups"
            overflowToDisk="true"
            maxElementsInMemory="300"
            diskPersistent="false"
            eternal="true">
        <!-- Uncomment the following to help debug cluster messaging -->
        <!--cacheEventListenerFactory class="org.jahia.services.cache.ehcache.LoggingCacheEventListenerFactory"
                                   properties=""/-->
                <cacheEventListenerFactory class="net.sf.ehcache.distribution.jgroups.JGroupsCacheReplicatorFactory"
                                   properties="replicateAsynchronously=true, replicatePuts=true, replicateUpdates=true, replicateUpdatesViaCopy=false, replicateRemovals=true"/>
    </cache>
    <cache
            name="LockPrerequisitesResultMap"
            overflowToDisk="true"
            maxElementsInMemory="300"
            diskPersistent="false"
            eternal="true">
        <!-- Uncomment the following to help debug cluster messaging -->
        <!--cacheEventListenerFactory class="org.jahia.services.cache.ehcache.LoggingCacheEventListenerFactory"
                                   properties=""/-->
                <cacheEventListenerFactory class="net.sf.ehcache.distribution.jgroups.JGroupsCacheReplicatorFactory"
                                   properties="replicateAsynchronously=true, replicatePuts=true, replicateUpdates=true, replicateUpdatesViaCopy=false, replicateRemovals=true"/>
    </cache>
    <cache
            name="LockPrerequisitesResultMap-groups"
            overflowToDisk="true"
            maxElementsInMemory="300"
            diskPersistent="false"
            eternal="true">
        <!-- Uncomment the following to help debug cluster messaging -->
        <!--cacheEventListenerFactory class="org.jahia.services.cache.ehcache.LoggingCacheEventListenerFactory"
                                   properties=""/-->
                <cacheEventListenerFactory class="net.sf.ehcache.distribution.jgroups.JGroupsCacheReplicatorFactory"
                                   properties="replicateAsynchronously=true, replicatePuts=true, replicateUpdates=true, replicateUpdatesViaCopy=false, replicateRemovals=true"/>
    </cache>

    <cache
            name="nodeusersacls"
            overflowToDisk="true"
            maxElementsInMemory="300"
            diskPersistent="false"
            eternal="true">
        <cacheEventListenerFactory class="net.sf.ehcache.distribution.jgroups.JGroupsCacheReplicatorFactory"
                                   properties="replicateAsynchronously=true, replicatePuts=true, replicateUpdates=true,
                                   replicateUpdatesViaCopy=false, replicateRemovals=true"/>
    </cache>
    <cache
            name="CJHTMLCache"
            overflowToDisk="true"
            maxElementsInMemory="10000"
            diskPersistent="true"
            timeToIdleSeconds="10"
            timeToLiveSeconds="14400"
            maxElementsOnDisk="50000"
            eternal="false">
        <cacheEventListenerFactory class="net.sf.ehcache.distribution.jgroups.JGroupsCacheReplicatorFactory"
                                   properties="replicateAsynchronously=true, replicatePuts=true, replicateUpdates=true,
                                   replicateUpdatesViaCopy=false, replicateRemovals=true"/>
    </cache>
    <cache
            name="CJHTMLCachedependencies"
            overflowToDisk="true"
            maxElementsInMemory="500"
            diskPersistent="true"
            eternal="true">
        <cacheEventListenerFactory class="net.sf.ehcache.distribution.jgroups.JGroupsCacheReplicatorFactory"
                                   properties="replicateAsynchronously=true, replicatePuts=true, replicateUpdates=true,
                                   replicateUpdatesViaCopy=false, replicateRemovals=true"/>
    </cache>
</ehcache>
