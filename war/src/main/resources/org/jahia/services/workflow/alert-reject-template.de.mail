to: #if( $user.properties.get('j:email') ) ${user.properties.get('j:email')} #end
to-users: nextTaskAssignable
subject: Veröffentlichungsworkflow abgewiesen von ${currentUser.getUsername()}
text: Hi,

Ein Veröffentlichungsworkflow mit einem Validierungsschritten wurde am ${date.format("short","short",$submissionDate,$locale)} zurückgewiesen von ${currentUser.getUsername()} :
#foreach( $node in $nodes )
#if( $velocityCount <= 10)
    - ${node.getDisplayableName()}
#end
#end

html: Hi,
<br>
<br>
<div>
Ein Veröffentlichungsworkflow mit einem Validierungsschritten wurde am ${date.format("short","short",$submissionDate,$locale)} zurückgewiesen von ${currentUser.getUsername()} :
</div>
<div>
    <ul>
#foreach( $node in $nodes )
    #if( $velocityCount <= 10)
    <li>${node.getDisplayableName()} (${node.getPath()})</li>
    #end
#end
    </ul>
    <br>
</div>
